@namespace("no.nav.paw.arbeidssokerregisteret.api.v1")
protocol JaNeiVetIkke {
	/**
	 * Enkel enum som brukes til typisk 'ja', 'nei' eller 'vet ikke' svar.
	 */
	enum JaNeiVetIkke {
		JA,
		NEI,
		VET_IKKE
	}
}