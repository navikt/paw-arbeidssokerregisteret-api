apiVersion: nais.io/v1
kind: Naisjob
metadata:
  name: paw-arbeidssokerregisteret-api-schema-registry
  namespace: paw
  labels:
    team: paw
spec:
  liveness:
    path: /isalive
  readiness:
    path: /isready
  image: {{ image }}
  kafka:
    pool: nav-dev
